def find_tuple_length(input_tuple):
    length = len(input_tuple)
    return length

my_tuple = (1, 2, 3, 4, 5)
result = find_tuple_length(my_tuple)

print(f"The length of the tuple {my_tuple} is: {result}")