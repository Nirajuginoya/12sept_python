def find_tuple_length(input_tuple):
    length_of_tuple = len(input_tuple)
    return length_of_tuple

example_tuple = (1, 2, 3, 'a', 'b', 'c')

tuple_length = find_tuple_length(example_tuple)

print(f"The length of the tuple is: {tuple_length}")